import React, { useEffect, useRef, useState } from "react";
import styled from "styled-components";

const Wrapper = styled.div``;

const AboutMe = () => {
  return <Wrapper>{"구현 중입니다. :)"}</Wrapper>;
};

export default AboutMe;
